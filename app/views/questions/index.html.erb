<% @questions.each do |question| %>
<div class="question row clearfix">
  <h2 style="margin: 0;">
    <%= link_to question.title, question %><br/>
    <small class="author" style="font-size: 1em">created <%= time_ago_in_words(question.created_at) %> by <%= question.user.name %></smail>
      <% end %>

      <br>

      <%= link_to 'New Question', new_question_path %>
